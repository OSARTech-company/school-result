# Backend Branches

This project keeps runtime backend code in `student_scor.py` and groups maintenance by role:

- `super/`
- `school/`
- `teacher/`
- `student/`
- `shared/`

Use these folders to store extracted route/service modules over time.
Current source of truth is `student_scor.py`.
